<!--#start 500 -->
<config>
<CreateProcess>AsUser</CreateProcess>
<vhs>
<env dev='{{$dev}}'/>
<vh_database
driver='{{$driver}}'
col_map='{{$col_map}}'
host='{{$db.host}}'
port='{{$db.port}}'
user='{{$db.user}}'
password='{{$db.passwd}}'
db='{{$db.dbname}}'
load_sql="{{$load_sql}}"
flush_sql=" {{$flush_sql}}"
load_host_sql="{{$load_host_sql}}"
/>
</vhs>
</config>
